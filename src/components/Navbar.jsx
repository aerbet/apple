import { appleImg } from "../utils";


const Navbar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} alt="appleImg"/>
      </nav>
    </header>
  );
}

export default Navbar;